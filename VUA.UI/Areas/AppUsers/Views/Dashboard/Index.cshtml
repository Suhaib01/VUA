@model VUA.Core.Models.AppllicationUser

@if (Model.StudantOrTeacher == "Studant")
{
    if(Model.IsApprovedUser == true)
    {

        <div id="overlay"></div>
        <div id="floating-screen" class="page-header">
            <!-- Content of your floating screen goes here -->
            <h4>Add Cours Now !</h4>
            <p>One step left to start</p>
            <a asp-action="AddCourse" class="btn btn-success">Add</a>
            <a asp-action="Profile" class="btn btn-outline-warning">Cancle</a>
        </div>



    }
    else
    {
        if (Model.IsCompleted ==true)
        {
            <div class="alert alert-primary mb-14 ">Your request has not been approved yet <a class="btn btn-success" asp-action="Chat"> Click here</a> if you want help.</div>
        }
        else{
            <div id="overlay"></div>
            <div id="floating-screen" class="page-header">
                <!-- Content of your floating screen goes here -->
                <h4>Confirm your account</h4>
                <p>Please complete your account information</p>
                <a asp-action="CompleteTeacherProfile" class="btn btn-success">Go it</a>
            </div>

        }



    }


}
else
{
    if (Model.IsApprovedUser == true )
    {

    }
    else
    {
        if(Model.IsCompleted == true)
        {
            <div class="alert alert-primary mb-14 ">Your request has not been approved yet <a class="btn btn-success" asp-action="Chat"> Click here</a> if you want help.</div>
        }
        else
        {
            <div id="overlay"></div>
            <div id="floating-screen" class="page-header">
                <!-- Content of your floating screen goes here -->
                <h4>Confirm your account</h4>
                <p>Please complete your account information</p>
                <a asp-action="CompleteTeacherProfile" class="btn btn-success">Go it</a>
            </div>
        }
        
    }
    
}

<style>
        /* Styling for the overlay */
        #overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.9); /* Semi-transparent black */
            z-index: 1; /* Place it above the rest of the content */
            display: none; /* Initially hidden */
        }

        /* Styling for the floating screen */
        #floating-screen {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            height: 160px;
            width: 500px;
            background-color: white;
            border: 1px solid #ccc;
            padding: 20px;
            z-index: 2; /* Place it above the overlay */
            display: none; /* Initially hidden */
        }

    </style>
<script>
        function openFloatingScreen() {
            document.getElementById('overlay').style.display = 'block';
            document.getElementById('floating-screen').style.display = 'block';
        }

        // Function to close the floating screen and hide the overlay
        function closeFloatingScreen() {
            document.getElementById('overlay').style.display = 'none';
            document.getElementById('floating-screen').style.display = 'none';

        }
        openFloatingScreen();

    </script>